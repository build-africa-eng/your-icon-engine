export type IconSource = 'lucide' | 'docker'; export interface UtilityRule { match: string | RegExp; icon: string; source: IconSource; } export const utilityRules: UtilityRule[] = [ { match: /^bg-/, icon: 'Palette', source: 'lucide' }, { match: /^text-/, icon: 'Type', source: 'lucide' }, { match: /^docker-node/, icon: 'docker-node', source: 'docker' }, ]; export function getMatchedIcon(utility: string): UtilityRule | null { for (const rule of utilityRules) { if (typeof rule.match === 'string' ? rule.match === utility : rule.match.test(utility)) return rule; } return null; }